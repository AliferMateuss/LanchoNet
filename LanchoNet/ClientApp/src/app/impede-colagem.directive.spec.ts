import { ImpedeColagemDirective } from './impede-colagem.directive';

describe('ImpedeColagemDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpedeColagemDirective();
    expect(directive).toBeTruthy();
  });
});
